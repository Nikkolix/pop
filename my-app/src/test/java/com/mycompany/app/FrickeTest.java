package com.mycompany.app;

import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

import java.io.IOException;

public class FrickeTest {

    @Test
    public void Example1() throws IOException, PlannerUndoableException {
        /*FileReader f = new FileReader("src/test/resources/Fricke1.txt");
        Planner planner = new Planner(f.readOperations(), Planner.toActionEffects(f.readS0()), Planner.toActionPreconditions(f.readSz()));
        planner.planSteps();*/
    }

    @Test
    public void Example2() throws IOException, PlannerUndoableException {
        /*FileReader f = new FileReader("src/test/resources/Fricke2.txt");
        Planner planner = new Planner(f.readOperations(), Planner.toActionEffects(f.readS0()), Planner.toActionPreconditions(f.readSz()));
        planner.planSteps();*/

    }

    @Test
    public void Example3() throws IOException, PlannerUndoableException {
        /*FileReader f = new FileReader("src/test/resources/Fricke3.txt");
        Planner planner = new Planner(f.readOperations(), Planner.toActionEffects(f.readS0()), Planner.toActionPreconditions(f.readSz()));
        planner.planSteps();*/

    }

    @Test
    public void Example4() throws IOException, PlannerUndoableException {
        /*FileReader f = new FileReader("src/test/resources/Fricke4.txt");
        Planner planner = new Planner(f.readOperations(), Planner.toActionEffects(f.readS0()), Planner.toActionPreconditions(f.readSz()));
        planner.planSteps();*/

    }

    @Test
    public void Example5() throws IOException, PlannerUndoableException {
        /*FileReader f = new FileReader("src/test/resources/Fricke5.txt");
        Planner planner = new Planner(f.readOperations(), Planner.toActionEffects(f.readS0()), Planner.toActionPreconditions(f.readSz()));
        planner.planSteps();*/
    }


}
